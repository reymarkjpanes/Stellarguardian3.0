# Sprint 8.2 Tasks (Stellar SDK & Domain Refinements)

- `[ ]` Database Migration (`000049_module8_refinements.sql`)
  - `[ ]` Add `Partially Completed` and `Broadcasting` to `payout_batch_status`.
  - `[ ]` Add `Finalized` to `payout_instruction_status`.
  - `[ ]` Add `Revoked` to `wallet_verification_status`.
  - `[ ]` Add fee accounting (`fee_asset`, `network_fee`, `provider_fee`, `total_fee`, `fee_payer`) to `payout_batches`.
  - `[ ]` Update RPCs (or create new ones) for simulation, batch completion, and settlement reconciliation.
  - `[ ]` Run `db reset` and `gen types`.
- `[ ]` Domain Models & Infrastructure
  - `[ ]` Extend `EscrowProvider` (Capabilities, ProviderIdentity, Simulation).
  - `[ ]` Define `TransactionSigner` abstraction and `EnvKeyManager` implementation.
  - `[ ]` Add Structured Logger specifically for Escrow Operations.
- `[ ]` StellarEscrowAdapter
  - `[ ]` Install `@stellar/stellar-sdk`.
  - `[ ]` Phase 1: Read-only logic (`getEscrowStatus`, `verifyFunding`, `getTransactionStatus`).
  - `[ ]` Phase 2: Signing & Simulation (`simulatePayoutBatch`).
  - `[ ]` Phase 3: Broadcast & Execution (`executePayoutBatch`).
- `[ ]` Escrow Service Updates
  - `[ ]` Integrate `simulatePayoutBatch` before broadcast.
  - `[ ]` Handle `Partially Completed` status during partial failures.
  - `[ ]` Implement immutable `reconcileSettlement(batchId)` workflow.
- `[ ]` Testing & Observability
  - `[ ]` Unit tests for partial batch success transitions.
  - `[ ]` Unit tests for Settlement creation (ensuring all instructions are terminal).
  - `[ ]` Test read-only calls against real Stellar Testnet (optional live test).
